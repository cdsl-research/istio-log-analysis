input {
    file {
        path =>["istio-log-dataset.csv.out.parsed"],
        codec => "json"
    }
}

#filter {
#
#}

output {
    elasticsearch {
        hosts => ["http://localhost:9200"],
        user => "elastic",
        password => "elastic",
        index => "istio"
    }
}
